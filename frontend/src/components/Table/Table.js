export default function Table() {
  return (
    <table className="table table-hover">
      <thead>
        <tr>
          <th>Tabla</th>
        </tr>
      </thead>
      <tbody>
        {/* {items.map((item) => (
          <tr key={item.id}>
            <td>{item.name}</td>
          </tr>
        ))} */}
      </tbody>
    </table>
  );
}
